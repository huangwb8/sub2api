# Changelog

**重要**：本文件是项目变更的**唯一正式记录**。凡是项目的更新，都要统一在本文件里记录。这是项目管理的强制性要求。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased]

### Added（新增）
- 新增了 `docs/plans/2026-04-24-upstream-6449da-to-1ce9dc-optimization-plan.md`：基于上游 `6449da6c..1ce9dc03` 的提交区间，沉淀当前 fork 对 Channel Monitor、Available Channels、RPM 限流、OpenAI/Codex 兼容、403 冷却、支付 webhook 与 license 同步状态的选择性吸收计划。
- 新增了 OpenAI 403 连续失败计数缓存与回归测试：OpenAI 账号遇到偶发 403 时先进入短时临时不可调度，连续达到阈值后才正式标记错误，降低风控抖动导致的误杀风险。
- 新增了网关 RPM 限流最小闭环：支持用户级与分组级 RPM 配置字段、Redis 分钟窗口计数、Claude/OpenAI/Gemini/Antigravity 网关认证后限流，以及对应迁移与单元测试；字段语义为 `NULL` 未配置、`0` 不限制、正数限流。

### Changed（变更）
- 融合了管理控制台 Dashboard 的“定价策略测算”和“超售数学测算”：现在统一为“套餐定价测算”大面板，按成本参数、用户参数、利润与风险分组输入，并集中展示达标套餐价格、预测月利润、保守月成本、安全垫和套餐价格换算。
- 简化了管理控制台 Dashboard 的“超售数学测算”：移除“目标盈利总额”输入和按总利润折算的达标价约束，达标套餐价格现在只按风险调整成本、目标盈利率、盈利口径与把握度推导。
- 重构了管理控制台 Dashboard 的“定价策略测算”结果区：移除独立的“达标月费单价”结果卡，改为在“套餐 · 达标单价”表格中展示月费等价、当前单价、达标单价与差额，并用轻量摘要 chips 承载用户数、保守成本和当前利润。
- 压缩了管理控制台 Dashboard 测算面板的参数输入框尺寸：统一减少卡片高度、间距与内边距，让“测算用户数 / 目标盈利率 / 盈利口径 / 置信水平”等参数排版更紧凑。
- 简化了管理控制台 Dashboard 的“定价策略测算”：移除核心表单中的“目标月利润”输入，达标月费单价现在只按测算用户数、目标盈利率、盈利口径与置信水平推导，减少与目标盈利率并列造成的定价歧义。
- 调整了账号配额用量更新后的调度刷新条件：总额度、日额度、周额度跨越限额，以及日/周窗口自动重置时都会触发调度 outbox，避免快照在额度边界漂移。
- 强化了 Codex Spark 模型限制提示：`gpt-5.3-codex-spark` 请求会注入图片能力限制说明，并拒绝图片输入或 `image_generation` 工具请求，避免把模型能力限制误判为本地工具缺失。

### Fixed（修复）
- 修复了管理控制台 Dashboard 参数说明气泡定位异常的问题：`HelpTooltip` 现在使用纯视口坐标定位，并会在左右边界与顶部空间不足时自动避让，避免“定价策略建议 / 超售数学测算”等面板里的问号说明框错位显示。
- 修复了支付 webhook 签名验证成功但订单不存在时返回 500 的问题：未知订单现在使用 sentinel error 分类并返回 provider 所需的成功 ack，仍保留告警日志且不触发履约。

## [1.0.27] - 2026-04-24

### Added（新增）
- 新增了 OpenAI `gpt-5.5` 支持：后端现已补齐默认模型暴露、OAuth/Codex 归一化、动态/静态计费兜底、兼容层 prompt cache key 自动注入与前端白名单/常用映射，管理员可以在不改变现有 GPT-5.x 默认策略的前提下安全启用新模型。

### Changed（变更）
- 优化了管理控制台 Dashboard 的”定价策略建议 / 超售数学测算”业务逻辑：两块面板都改为以管理员指定的用户数做客观测算，移除了前端内置的推荐参数组合与最低用户数推导，改为展示保守成本、达标单价、当前定价利润与套餐折算结果，流程更直接且不改变既有后端接口。

## [1.0.26] - 2026-04-23

### Added（新增）
- 新增了分组级”闲时动态计费”能力：管理员现在可以按北京时间配置精确到秒的闲时时间窗，并为该时间窗单独设置闲时倍率与闲时额外盈利率；相关配置已贯通后端分组模型、认证缓存快照、实际网关计费链路、管理端分组表单与必要测试。

### Changed（变更）
- 优化了 `docs/page-demo/2026-04-13-benszresearch-homepage-reference.html` 这份首页参考模板：左上角品牌位现在会优先读取后台公开设置中的 `site_logo`，右下角法律链接同步对齐到管理员在”用户默认设置”中维护并公开发布的 `/legal/terms` 与 `/legal/privacy` 页面；未发布对应文档时会自动隐藏入口，减少示例页与真实站点配置脱节。
- 吸收了上游支付正确性强化中的低风险核心能力：支付订单现在会在创建时固定记录 `provider_instance_id / provider_key / provider_snapshot`，支付结果页公开校验接口同步补齐手续费与订单阶段信息，便于在不影响现有个性化支付/订阅流程的前提下提升历史订单可追溯性与结果页一致性。

### Fixed（修复）
- 修复了支付成功回调仍可能”串 provider”或误认微信商户的风险：后端现在会按订单绑定的 provider 进行来源校验，并对微信支付回调/补单结果额外校验 `appid / mchid / currency / trade_state` 与订单快照是否一致，降低配置变更或多实例并存时的误履约概率。
- 修复了历史订阅补差价升级被错误拦截的问题：订阅升级现在会在源套餐 `upgrade_family` 缺失或历史套餐行已不存在时，回退复用同分组内一致的升级元数据，并在发现同分组配置冲突时显式拒绝，恢复旧订阅升级能力且避免误放开错误升级链。

## [1.0.25] - 2026-04-22

### Changed（变更）
- 重构了管理控制台 Dashboard 的"定价策略建议"和"超售数学测算"两个面板的前端 UI：统一视觉语言，与同页其它 `card p-5` 面板风格对齐；保留所有 `data-testid` 与 i18n key 不变，功能行为与数据绑定零变更。
  - 面板头部新增色调图标 + 标题 + 描述的复合结构，徽章组统一靠右排列，响应式下自动堆叠。
  - 用 8 个独立边框框表单的散乱布局替换为统一灰底容器 + 原生 `.input` 类的有组织输入区，视觉噪声显著降低。
  - 关键结果卡（推荐单价 / 最低用户数 / 每用户利润 等）改为"白底 + 左侧主色条 + 单色原点"的克制样式，替换原先高饱和浅色背景块，避免"彩虹色"视觉冲突。
  - 内部分区引入 `<h4>` 小节标题与 `mt-5 space-y-5` 节奏，信息层级更清晰。
  - 表格沿用一致的 `rounded-2xl`、`hover:bg-gray-50`、`transition-colors` 与深色模式 token，保持与页面其它表格视觉呼应。

## [1.0.24] - 2026-04-22

### Added（新增）
- 新增了 `docs/plans/2026-04-22-upstream-23def40-to-78f691-optimization-plan.md`：基于上游 `23def40b..78f691d2` 的提交区间，沉淀对当前 fork 在微信支付配置前置校验、支付错误国际化、OpenAI 模型矩阵治理与 License 跟随判断上的选择性吸收计划。

### Changed（变更）
- 调整了支付错误处理链路：后端微信支付配置改为在保存启用中的服务商实例时就执行结构化校验并透传 `reason/metadata`，前端支付相关页面统一改为优先按 `payment.errors.*` 做国际化渲染，同时保留现有支付流程、服务商排序与个性化 UI 逻辑不变。
- 更新了 `docs/PAYMENT.md` 与 `docs/PAYMENT_CN.md`：补充微信官方服务商”保存即校验”的后台行为说明，以及常见字段/PEM 配置错误的排查提示。

### Fixed（修复）
- 修复了微信支付配置”保存时静默、下单时爆炸”的问题：现在缺少 `publicKeyId/certSerial`、`APIv3 Key` 长度错误或 PEM 非法都会在后台保存启用实例时立即暴露，不再拖到用户首单。
- 修复了支付错误国际化链路不完整的问题：支付页面和管理端支付页面现在会优先消费后端返回的结构化 `reason/metadata`，不再依赖局部手写分支或单一 `PENDING_ORDERS` 映射。
- 修复了 OpenAI 兼容守卫对非 GPT-5/Codex 模型的潜在误判风险：`prompt_cache_key` 自动注入与 GPT-5.4 长上下文倍率判定现在都先经过显式前缀守卫，避免未来模型 fallback 调整时误伤你现有的模型矩阵。

## [1.0.23] - 2026-04-21

### Added（新增）
- 新增了 `docs/超售的数学原理.md`：用可计算的超售模型解释用户需求分布、保本单价与用户池规模之间的关系，并给出在 20% 盈利目标下的最小招揽用户数建议。
- 新增了管理后台 Dashboard 定价策略顾问面板：基于近 30 天订阅用量样本自动估算用户消耗分布，按"成本加成 / 净利率"两种模式计算建议月费、最低用户数与安全缓冲。

## [1.0.22] - 2026-04-20

### Changed（变更）
- 统一了账号可调度性的配额语义：API Key / Bedrock 账号现在会把额度超限纳入 `Account.IsSchedulable()` 单一真相，粘性会话清理与后台状态展示同步复用这套判定，减少调度、sticky 与 UI 三处语义漂移。
- 调整了 Claude `reasoning effort` 展示：前端使用记录中的 `xhigh` 现在统一展示为 `XHigh`，与后端新增的 `xhigh` 归一化兼容保持一致。

### Fixed（修复）
- 修复了删除账号后 `scheduled_test_plans` 可能残留孤儿记录的问题：账号删除事务现已显式清理关联定时测试计划，避免软删除场景下数据库级联失效留下脏数据。
- 修复了 Claude Messages `output_config.effort=xhigh` 在当前 fork 中无法被后端识别的问题：网关请求解析、使用记录归一化与相关测试现已补齐。
- 修复了后台账号状态对 API Key / Bedrock 配额超限的提示不清问题：不可调度且已超限的账号现在会明确显示”配额超限”，不再和普通手动暂停混淆。

## [1.0.21] - 2026-04-20

### Added（新增）
- 新增了 `docs/plans/2026-04-20-upstream-51af8df-to-23def40-optimization-plan.md`：基于上游 `51af8df3..23def40b` 的提交区间，沉淀对当前 fork 在配额调度一致性、定时测试计划清理、Claude `xhigh` 兼容与许可证同步上的选择性吸收计划。

### Changed（变更）
- 重构了 README 系列（`README.md`、`README_EN.md`、`README_JA.md`）：从原作者版本改为 fork 身份，新增致谢上游、fork 特色对比、Star 引导章节，移除上游赞助商与官方域名声明，统一部署链接为 fork 仓库地址，三语言结构与关键信息保持一致。
- 同步了上游许可证调整：根目录 `LICENSE` 从 `MIT License` 更新为 `GNU Lesser General Public License v3.0`，与 upstream `23def40b` 保持一致。
- 调整了 OpenAI API Key 自定义 `base_url` 的 Responses 端点拼接策略：官方 `api.openai.com` 根地址继续走 `/v1/responses`，而第三方兼容上游在未显式填写 `/v1` 时改为按字面 `base_url + /responses` 组装，避免把所有非官方地址一律强行补成 `/v1/responses`。
- 重构了盈利面板订阅成本回退查询：`/api/v1/admin/dashboard/profitability` 现在会基于查询时间窗内的账号 `actual_cost_cny` 与该账号窗口总 `actual_cost` 做比例分摊，不再依赖会持续漂移的 `accounts.actual_cost_usage_usd` 作为累计分母。

### Fixed（修复）
- 修复了 OpenAI API Key 账号”后台测试能通过、真实网关请求却失败”的路径不一致问题：账号测试链路现在复用与真实网关一致的 Responses URL 构造逻辑，避免第三方上游因 `/responses` 与 `/v1/responses` 语义不兼容而出现假阳性测试结果。
- 修复了盈利面板真实成本失真的问题：订阅请求的成本展示现在按所选时间范围内的账号真实成本占比分摊，缓解了历史累计分母导致的早期成本虚高与跨周期混淆。

## [1.0.20] - 2026-04-19

### Changed（变更）
- 恢复了 OpenAI 账号 `WS mode` 的 `ctx_pool` 前端可选项：创建账号、编辑账号与批量编辑现在都会展示完整的 `off / ctx_pool / passthrough` 三态选项，避免已配置 `ctx_pool` 的账号在编辑时出现下拉框空白。
- 补充了 `docs/关键模型参数设置.md` 中 API Key 账号错误处理参数的教程：新增 `池模式`、`自定义错误码`、`临时不可调度` 的推荐配置、互斥关系与典型场景，便于管理员按”官方直连 / 上游号池 / 第三方代理”三类来源选择更稳妥的默认值。
- 调整了支付 provider 配置存储与读取路径：新写入改为明文 JSON，读取端改为”JSON 优先 + 旧 AES 密文兼容回退 + 坏数据 fail-open 为空配置”，避免支付配置依赖 `TOTP_ENCRYPTION_KEY` 才能在重启后读回。
- 优化了支付前端交互：支付宝官方直连桌面端改为纯跳转收银台，不再把收银台 URL 当作二维码渲染；支付弹窗尺寸改为按可用屏幕动态计算，并把前端识别的 `is_mobile` 显式传给后端以提升 H5/PC 分流准确性。
- 提高了网关默认上游响应体读取上限：`gateway.upstream_response_read_max_bytes` 的默认值从 `8MB` 提升到 `128MB`，并收口到配置层共享常量，减少大图/多模态响应被误判为上游异常的概率。

### Fixed（修复）
- 修复了 OpenAI `ctx_pool` 模式在管理后台被前端隐藏的问题：此前 `EditAccountModal`、`CreateAccountModal` 中的选项被注释掉，`BulkEditAccountModal` 也缺少该选项；现已统一补齐并新增回归测试覆盖，防止后续再次回退。
- 修复了管理端支付 provider 配置泄露敏感字段的问题：服务端现在会按 provider 类型剔除 `privateKey`、`secretKey`、`apiV3Key`、`webhookSecret` 等写入型凭据，编辑时空敏感字段默认表示”保持原值”。
- 修复了订阅计费绕过倍率的问题：订阅配额消耗改为按 `ActualCost` 结算，分组倍率、用户专属倍率以及 `0x` 免费订阅现在会真实反映到订阅用量。
- 修复了管理员编辑 API Key 账号时浏览器密码管理器误填的问题：账号 API Key 输入框已补充 `autocomplete=”new-password”` 以及 1Password / LastPass / Bitwarden 的忽略属性。

## [1.0.19] - 2026-04-19

### Added（新增）
- 新增了 `docs/汇率波动如何影响用户购买行为与权益.md`：面向开发与运营解释本项目中人民币余额、美元 usage、订阅套餐与汇率波动之间的关系，并补充按量扣费、汇率快照、订阅快照与补差价升级的源码级成因说明，帮助统一用户沟通口径。
- 新增了 `docs/管理员参数设置最佳实践.md`：面向管理员梳理系统参数配置的最佳实践与推荐值。

### Changed（变更）
- 更新了 `AGENTS.md` 与 `CLAUDE.md` 的文档维护约定：新增”代码变更时必须同步更新 `docs/` 下非计划类文档”的默认规则，要求用户文档、教程与操作说明持续和最新源码保持一致。
- 调整了 `.gitignore` 的文档白名单：允许新建的 `docs/汇率波动如何影响用户购买行为与权益.md` 被 Git 正常跟踪，避免教程文档因 `docs/*` 默认忽略规则而无法入库。
- 补强了 `docs/汇率波动如何影响用户购买行为与权益.md` 的结论部分：新增”当前汇率策略更偏向谁”的说明，并明确这套保守汇率机制的目标是保护平台利润稳定与长期运营，而不是通过汇率处理压榨用户。
- 补强了 `docs/汇率波动如何影响用户购买行为与权益.md` 的实现说明：新增”系统如何稳定、可靠获取汇率”的章节，明确默认参数、进程内缓存、`singleflight`、请求超时、失败回退与最近成功汇率持久化等机制。
- 调整了汇率缓存默认值与系统设置入口：`billing.fx.cache_ttl_seconds` 的默认值从 600 秒改为 86400 秒（24 小时），并在管理后台”系统设置”中新增可直接修改该缓存时长的表单入口。

## [1.0.18] - 2026-04-19

### Added（新增）
- 新增订阅类型 usage 的估算成本计算：后端在订阅请求的 usage 记录中现在会自动写入 `estimated_cost_cny`，填补了此前订阅成本统计始终为零的缺口。
- 新增迁移 `109_backfill_subscription_profitability_cost.sql`：幂等回填历史订阅 usage 的估算成本，使盈利面板能展示完整的订阅成本曲线。
- 新增盈利趋势图"额外盈利率"降级提示：当所选区间没有可计算成本时，面板不再显示空白利润率，而是给出"当前区间没有可计算成本"的友好提示。

### Changed（变更）
- 重构了盈利能力核心计算逻辑：将估算成本计算抽取为独立函数 `resolveProfitabilityEstimatedCostCNY`，订阅和余额两种计费类型复用同一成本估算路径。
- 优化了前端盈利趋势图：图表数据集定义重构为类型化的 `ProfitabilityChartDataset` 接口，额外盈利率线段改为仅在存在有效数据时渲染，避免无数据时出现无效折线。

## [1.0.17] - 2026-04-18

### Added（新增）
- 新增了服务条款与隐私政策的独立公开页面：管理员现在可以在”系统设置 - 用户默认值”中维护两份 Markdown 文档，站点会生成稳定的 `/legal/terms` 与 `/legal/privacy` 入口供首页或外部页面直接链接。

### Changed（变更）
- 扩展了首页与系统设置页：默认首页页脚会在配置了法律文档内容后自动展示”服务条款 / 隐私政策”入口，后台”用户默认值”分区同步加入公开链接预览、复制和文档编辑能力，便于一处维护、到处复用。

## [1.0.16] - 2026-04-18

### Added（新增）
- 新增了 `docs/关键模型参数设置.md`：整理账号管理、分组管理与 OpenAI `/v1/messages` 调度相关关键参数的含义、推荐设置方式与常见使用场景，便于管理员按”稳定性 / 成本 / 协议兼容 / 合规”维度统一配置口径。
- 新增了 `plans/2026-04-18-upstream-v2-v1-optimization-plan.md`：基于上游 `ad64190..6c73b621` 的提交区间沉淀选择性吸收计划，明确当前 fork 已吸收的大主题、仍值得补的 correctness/stability 缺口，以及后续按主题最小移植的实施顺序。

### Changed（变更）
- 调整了 `.gitignore` 的文档白名单：允许新建的 `docs/关键模型参数设置.md` 被 Git 正常跟踪，避免关键运营教程因 `docs/*` 默认忽略规则而无法入库。

## [1.0.15] - 2026-04-18


### Added（新增）
- 新增了公开订阅套餐接口 `GET /api/v1/payment/public/plans`：首页、自定义落地页等未登录场景现在也可以只读获取后台已上线的套餐列表，用于自动化展示费用页。
- 新增了订阅补差价升级能力：订阅套餐现在支持配置 `upgrade_family` / `upgrade_rank` 升级梯度，活跃订阅会持久化当前套餐快照，普通用户可在订阅页和购买页按剩余价值折抵升级到同升级族内更高档套餐。
- 新增了站内公开费用页 `/pricing`：未登录用户、自定义首页以及后台配置的“费用”入口现在都可以复用同一套公开套餐展示页查看管理员已上架的订阅费用。
- 新增了 `docs/plans/2026-04-18-dashboard-capacity-recommendation-remediation-plan.md`：沉淀管理控制台“加号推荐”从按订阅分组补号重构为按全站容量池推荐的整改方案，明确容量池建模、零活跃样本剔除、接口收口与前端面板改造顺序。

### Changed（变更）
- 更新了 `AGENTS.md` 与 `CLAUDE.md` 的远程排查约定：明确根目录 `remote.env` 用于保存远程测试站点的 `REMOTE_BASE_URL` / `REMOTE_ADMIN_API_KEY`，仅限本地只读排查，且真实凭据不得提交入库。
- 优化了首页自定义内容能力：`home_content` 的 HTML 模式现在支持受信任管理员内容中的内联脚本执行与清理机制，可用于实现多界面导航、动态套餐加载等轻量交互，同时保留 iframe 模式兼容。
- 升级了 `docs/page-demo/2026-04-13-benszresearch-homepage-reference.html`：在保留原有首页主视觉的基础上新增“首页 / 费用”双界面导航，并让费用页自动读取后台当前可售订阅套餐。
- 更新了支付与 README 文档：补充订阅补差价升级的业务规则、用户入口、历史无快照订阅限制以及退款需人工处理的边界说明，并同步对齐多语言 README 的能力描述。
- 调整了文档忽略规则：`docs/page-demo/` 目录不再被 `.gitignore` 排除，便于跟踪首页参考页等演示文档的实际改动。
- 调整了管理后台“加号推荐”接口与面板语义：`/api/v1/admin/dashboard/recommendations` 现在以“全站摘要 + 容量池列表”返回推荐结果，前端同步改为展示“可调度账号缺口 / 可恢复不可调度账号 / 容量池贡献套餐”，不再把订阅分组或套餐当作主推荐对象。

### Fixed（修复）
- 修复了订阅购买页从 URL 直达分组或升级目标时可能没有完整复用套餐选择逻辑的问题：现在会同步初始化合适的支付方式，并在目标套餐不可升级时给出明确提示，避免用户误入无反馈状态。
- 修复了管理员首次进入 `系统设置` 页就弹出 `setting not found` 告警的问题：根因是 `web_search_emulation_config` 在空库/旧库未初始化时被后端当作错误返回；修复方式是将该设置读取改为与其它系统设置一致的默认 fail-open 行为，并补充回归测试覆盖未配置场景。
- 修复了主页/自定义页面里的“费用”入口可能空白或打不开的问题：现在前端会直接渲染公开套餐页，站内相对路径 iframe 也会被正确转换并允许同源嵌入，确保管理员后台上线的套餐可以稳定展示给用户。
- 修复了严格 CSP 环境下自定义首页 `home_content` 内联脚本无法执行的问题：线上站点返回 `script-src 'self' 'nonce-...'` 时，动态插入的首页脚本此前没有继承 nonce，导致“费用”按钮点击无反应、套餐加载逻辑完全不运行；现在会复用当前文档的 CSP nonce 执行脚本，并补充回归测试覆盖该场景。
- 修复了管理后台“加号推荐”会被零活跃样本稀释、共享账号池重复推荐、以及把“恢复现有不可调度账号”和“新增账号”混成一个数字的问题：后端现已按共享账号连通分量推导容量池、只让真实活跃样本参与基线计算，并把推荐值拆分为“建议补充可调度账号 / 可恢复不可调度账号 / 仍需新增账号”，同时补齐容量池建模、接口契约和 Dashboard 前端回归测试。

## [1.0.10] - 2026-04-16

### Added（新增）
- 新增了 `docs/管理员参数设置最佳实践.md`：沉淀管理员在配置 OpenAI 分组“允许 `/v1/messages` 调度”以及“账号过滤控制”参数时的启用/关闭判断标准、默认模型映射含义与常见使用场景，便于运营和技术支持统一口径。
- 新增了 `plans/2026-04-15-dynamic-fx-billing-plan.md`：沉淀“在线汇率 + 最近成功汇率 + 7.2 底线汇率 + 安全加价” 的按量计费改造方案，明确人民币余额账本、美元 usage 成本、汇率快照落库与渐进式上线顺序，供后续审查与实施。
- 新增了余额盈利率结算与观察能力：账号管理支持配置“账号实际成本（CNY）”，分组管理为标准余额计费新增“额外盈利率(%)”，仪表盘新增“盈利水平”趋势面板，并补充后端盈利率聚合接口与 usage 成本快照字段。

### Changed（变更）
- 调整了前端货币格式化入口：新增显式的 `CNY/USD` 语义化格式化函数，并将余额、充值、支付、订单金额链路统一收口到人民币展示，同时让 usage、quota、rate limit 与 `*_usd` 限额链路显式保持美元展示。
- 更新了 README 与支付文档中的金额口径说明：明确用户余额/支付金额按 CNY 展示，API 成本与额度限制按 USD 展示，避免运营与用户混淆两套金额语义。
- 调整了按量计费结算链路：usage 仍按 USD 成本建模，但最终余额扣减改为基于“实时汇率 / 最近成功汇率 / 底线汇率 + 安全加价”计算 `charged_amount_cny`，并把汇率快照同步落到 usage log 与幂等扣费表中。
- 调整了前后台 usage 展示与导出：在保留 USD 成本明细的同时，新增人民币结算额与汇率快照的解释性展示，并补充订单/余额页面的金额语义提示，降低用户与管理员对账歧义。
- 调整了管理员订阅与导航结构：左侧导航顺序改为“账号管理 → 分组管理 → 渠道管理 → 订阅管理”，并将原“订单管理 - 订阅套餐”并入“订阅管理”页顶部，保留旧入口重定向兼容。

### Fixed（修复）
- 修复了人民币余额账本被美元按量成本直接扣减的风险：表现是充值/余额语义已统一为 CNY，但 usage 扣费仍可能直接按 `actual_cost` 数值减余额；修复方式是引入汇率服务、CNY 扣费快照、迁移与仓储层幂等补全，并在生产装配中注入真实汇率结算服务。
- 修复了管理员配置余额盈利策略时需要手算倍率和跨币种换算的问题：改为由系统根据账号实际成本、真实 token 用量、实时汇率与分组额外盈利率自动完成后台换算，避免手工倍率计算带来的理解偏差和运营负担。

## [1.0.8] - 2026-04-14

### Added（新增）
- 新增了 Anthropic API Key 场景的全局 `WebSearch` 模拟能力：后端增加搜索 provider 管理、代理绑定、配额统计与重置/测试接口，前端在管理员设置页新增独立配置卡片，默认关闭且仅在显式启用后生效。
- 新增了 `backend/migrations/103_add_allow_user_refund.sql`：为支付服务商实例补充 `allow_user_refund` 字段，用于细粒度控制用户是否可对该通道订单发起自助退款。

### Changed（变更）
- 调整了支付服务商配置链路：管理端现在支持单独配置”允许用户自助退款”，并在关闭服务商退款能力时自动联动关闭用户自助退款，避免后台配置出现语义漂移。
- 调整了网关启动时的 WebSearch 装配逻辑：只有在 provider、代理与配置都满足条件时才会构建搜索管理器，缺失代理映射的 provider 会被安全跳过，避免异常情况下静默直连。
- 调整了系统邮件发送的网络超时控制：SMTP plain/TLS 连接都改为显式拨号超时与 IO 超时，提升异常网络环境下的可恢复性。

### Fixed（修复）
- 修复了支付退款治理”半同步”导致的前后端不一致问题：表现是 schema 已出现 `allow_user_refund` 概念，但 Ent 生成代码、迁移、退款校验与用户侧按钮显隐未完全打通；修复方式是补齐 schema/迁移/服务/路由/UI 全链路并重新生成 Ent 与 Wire 代码。
- 修复了 WebSearch 新接入链路中的编译与依赖注入问题：表现是 `websearch_config.go` 缺少 `atomic` 导入、`wire` 依赖图存在重复/缺失 provider；修复方式是补齐 provider 包装层、改用稳定的 payment provider set，并重新生成 `wire_gen.go`。

## [1.0.7] - 2026-04-14

### Added（新增）
- 新增了 `plans/2026-04-14-api-gateway-growth-operations-playbook.md`：沉淀 Sub2API API 站从 10 人到 10 万用户的分阶段扩容教程，覆盖部署形态、服务器规格、安全基线、监控备份、专人维护与平台化演进建议。

### Changed（变更）
- 重构了根目录 README 文档体系：将 `README.md` 调整为中文主入口，按“项目定位 → 核心能力 → 快速部署 → 文档导航”重组内容，便于 fork 后持续维护时以中文文档为默认入口。
- 进一步调整了多语言 README 策略：删除 `README_CN.md`，新增 `README_EN.md` 作为英文入口，并将 `README_EN.md`、`README_JA.md` 的章节结构与关键信息统一向中文主 README 对齐。
- 在 `AGENTS.md` 中新增了 README 维护规则：明确 `README.md` 为中文主 README，要求其它语言 README 在结构、口径与链接上始终与中文主 README 保持同步。

### Fixed（修复）
- 修复了管理后台“加号推荐”在小分组中可能持续机械建议补号的问题：根因是推荐算法允许“单个账号承载少于 1 个活跃订阅/活跃用户”的过低计数基线，导致管理员按建议补号后在无新增订阅时仍可能继续收到加号建议；修复方式是将订阅数/活跃用户数基线统一收敛到至少 1，并补充回归测试覆盖补号后应停止继续加号的场景。

## [1.0.6] - 2026-04-14

### Added（新增）
- 新增了”管理员订阅通知邮箱”系统设置项：可在系统设置的邮件设置中单独配置接收新增用户订阅提醒的邮箱，留空时默认不发送通知。

### Changed（变更）
- 调整了订阅服务与邮件队列的协作方式：订阅通知改为仅使用系统设置中的专用通知邮箱，并继续通过异步队列投递，避免阻塞现有订阅开通链路。

### Fixed（修复）
- 修复了新增用户订阅提醒默认行为不够可控的问题：表现是未明确配置专用收件人时仍可能触发通知，修复方式是新增独立通知邮箱配置并在留空时完全禁用发送。

## [1.0.4] - 2026-04-13

### Added（新增）

### Changed（变更）
- 调整了用户订阅购买页的支付方式逻辑：订阅场景现在会按当前账户余额动态提供“余额支付”选项，并在站内余额直接购套餐成功后立即刷新余额与订阅状态，避免用户进入无意义的第三方支付等待态。

### Fixed（修复）
- 修复了用户无法使用账户余额购买订阅套餐的问题：根因是订阅订单链路只支持第三方支付通道、缺少站内余额结算与退款回补分支；修复方式是补充后端余额购订阅下单/履约/退款逻辑，并让前端仅在订阅场景暴露余额支付入口。

## [1.0.2] - 2026-04-13

### Added（新增）

### Changed（变更）
- 更新了支付文档与管理端微信配置语义：官方直连支付统一以 `alipay` / `wxpay` 作为顶层方式标识，并将微信直连所需的 `publicKeyId`、`certSerial` 明确为必填，避免表单与后端校验不一致。

### Fixed（修复）
- 修复了微信与支付宝官方直连服务商已配置但用户下单仍提示未配置的问题：修复方式是让后端 provider registry 直接按 `wxpay` / `alipay` 注册官方直连通道，并补充回归测试覆盖 Stripe 顶层映射不受影响。

## [1.0.1] - 2026-04-13

### Added（新增）

### Changed（变更）
- 调整了管理员侧边栏的支付入口显示逻辑：支付菜单现在会同时参考公开设置缓存与管理员支付配置，避免因加载时序或简易模式导致“订阅套餐”管理页入口缺失。
- 更新了订阅套餐管理页的交互提示与有效期单位兼容性：管理员现在可以直接识别“可售套餐”与订阅分组的关系，并可统一编辑天/周/月/年四类有效期单位。

### Fixed（修复）
- 修复了管理员创建订阅套餐后列表不立即显示的问题：修复方式是统一前后端套餐有效期单位并增强前端本地 upsert/reload 同步，避免保存成功但列表不刷新。
- 修复了历史套餐使用 `days/weeks/months` 等旧单位时可能导致用户自助订阅展示异常的问题：修复方式是新增前后端单位归一化兼容层，确保购买页、管理页与下单逻辑保持一致。

## [1.0.0] - 2026-04-13

### Added（新增）

- 新增了 `docs/plans/2026-04-13-user-self-service-subscription.md`：沉淀普通用户自助订阅闭环的实现计划、测试入口与验收步骤。
- 新增了 `docs/plans/2026-04-13-release-automation-and-docker-publish.md`：沉淀版本驱动 release、Docker 镜像自动补发和本地验证流程的实施计划。
- 新增了 `tools/verify-release-automation.sh` 与 `make verify-release-automation`：用于本地校验版本文件、发布 workflows 与部署文档是否保持一致。
- 新增了 `docs/GITHUB_REPOSITORY_SETUP_TUTORIAL.md`：面向仓库维护者，逐步说明如何在 GitHub 仓库里配置 Secrets、Variables、Actions 权限和发布流程。

### Changed（变更）

- 调整了内置支付的订阅商品暴露规则：仅激活且为订阅计费类型的分组可作为自助订阅套餐对外出售。
- 优化了用户支付等待页的状态恢复：当第三方 webhook 延迟或丢失时，会主动触发订单核验以尽快补齐订阅/充值到账。
- 更新了 `docs/PAYMENT.md` 与 `docs/PAYMENT_CN.md`：同步内置支付现已支持普通用户自助购买与续费订阅套餐。
- 更新了 `deploy/README.md` 与 `deploy/DOCKER.md`：补充 `backend/cmd/server/VERSION` 作为发布版本源、`create-release.yml`/`publish-release-images.yml` 的职责和仓库变量配置说明。
- 调整了 `.gitignore` 对文档目录的规则：放行 `docs/plans/*.md`，确保发布与实施计划文档可以纳入版本控制。
- 更新了 `docs/GITHUB_REPOSITORY_SETUP_TUTORIAL.md`：补充 Docker Hub Personal Access Token 的官方获取入口、逐步创建步骤与推荐权限说明。

### Fixed（修复）

- 修复了无效分组套餐仍可能出现在购买页的问题：表现是停用分组或非订阅分组套餐会被错误展示，修复方式是补充服务端校验与过滤。
- 修复了支付成功但 webhook 未及时到达时用户可能长时间卡在等待页的问题：修复方式是在前端等待态增加订单主动核验恢复。
- 修复了发布流程缺少版本同步与镜像补发兜底的问题：通过新增 `check-version-sync.yml`、`create-release.yml` 和 `publish-release-images.yml`，补齐版本校验、tag 驱动发布和 Docker 镜像自动补发链路。

## [0.1.0] - 2026-04-13

### Added（新增）

- 初始化 AI 项目指令文件
- 生成 `AGENTS.md`（跨平台通用项目指令，Single Source of Truth）
- 生成 `CLAUDE.md`（Claude Code 特定适配，通过 `@./AGENTS.md` 自动引用）
- 配置项目工程原则和工作流
- 记录项目技术栈：Go (Gin + Ent ORM) + Vue3 (TypeScript + Pinia + Tailwind CSS)
- 记录项目结构：backend（handler/service/repository 分层）、frontend（Vue3 SPA）、deploy、docs
- 记录关键开发注意事项：Ent Schema 变更流程、pnpm-lock.yaml 同步、Interface 变更补全、数据库迁移规范

---

## 记录规范（强制性要求）

### 必须记录的变更类型

每次修改以下内容时，**必须**在本文件追加记录：

1. **项目指令文件变更**
   - CLAUDE.md 的任何修改
   - AGENTS.md 的任何修改

2. **项目结构变更**
   - 新增/删除/重命名目录
   - 新增/删除/重命名关键文件（如核心源码文件、配置文件）

3. **工作流变更**
   - 核心工作流程的调整
   - 开发流程的修改

4. **工程原则变更**
   - 新增工程原则
   - 修改或删除现有工程原则

5. **重要配置变更**
   - 影响项目行为的配置文件修改
   - 依赖关系的重大变更

### 记录格式

```markdown
## [版本号] - YYYY-MM-DD

### Added（新增）
- 新增了 XXX 功能/章节：用途是 YYY

### Changed（变更）
- 修改了 XXX 章节：原因是 YYY，具体变更内容是 ZZZ
- 修改了项目目录结构：将 ABC 目录移至 DEF 位置

### Fixed（修复）
- 修复了 XXX 问题：表现是 YYY，修复方式是 ZZZ

### Deprecated（即将弃用）
- XXX 功能将在下一版本移除：原因是 YYY

### Removed（已移除）
- 移除了 XXX 功能：原因是 YYY

### Security（安全）
- 修复了 XXX 安全漏洞：影响是 YYY
```

### 记录时机

- **修改前**：先在 `[Unreleased]` 部分草拟变更内容
- **修改后**：完善变更描述，添加具体细节和影响范围
- **发布时**：将 `[Unreleased]` 内容移至具体版本号下

### 版本号规则

遵循语义化版本（Semantic Versioning）：
- **主版本号（Major）**：重大架构变更、不兼容的 API 修改
- **次版本号（Minor）**：新增功能或章节，向后兼容
- **修订号（Patch）**：修复问题或微调，向后兼容

### 变更类型说明

| 类型 | 说明 | 示例 |
|------|------|------|
| Added | 新增的功能或章节 | "新增了 `## 变更记录规范` 章节" |
| Changed | 对现有功能或内容的变更 | "修改了 `## 工程原则` 章节，增加了早期返回原则" |
| Deprecated | 即将移除的功能（警告） | "旧的目录结构将在下个版本重构" |
| Removed | 已移除的功能 | "移除了已废弃的 `## 代码审查` 章节" |
| Fixed | 修复的问题 | "修复了模板中目录树生成的 bug" |
| Security | 安全相关的修复 | "修复了依赖包的安全漏洞" |

### 质量标准

每条记录应该：
- **清晰具体**：说明改了什么、为什么改
- **可追溯**：包含足够的上下文信息
- **格式统一**：遵循上述模板
- **及时更新**：修改后立即记录，不要拖延
